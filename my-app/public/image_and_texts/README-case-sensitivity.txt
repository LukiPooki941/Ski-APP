Netlify (Linux) is case-sensitive.

This app references images via URLs like:
  /image_and_texts/BigHorn.jpeg

Those names must match the file names in this folder exactly.

Tip: If you ever rename images, keep a consistent naming convention (all-lowercase is easiest).
